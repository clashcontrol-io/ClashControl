name: Daily memory sync

on:
  schedule:
    # Runs at 06:00 UTC every day
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Re-run even if already updated today'
        required: false
        default: 'false'
        type: boolean

jobs:
  update-memory:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0          # full history so git log --since works
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run memory updater
        id: updater
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            python3 scripts/update-memory.py --force
          else
            python3 scripts/update-memory.py
          fi

      # A crash here previously left MEMORY.md silently stale with no signal
      # anywhere but a red X on a schedule nobody watches — this was the
      # actual root cause of the Project State line going stale for weeks.
      - name: Surface failure
        if: failure() && steps.updater.outcome == 'failure'
        run: |
          echo "### :x: Daily memory sync failed" >> "$GITHUB_STEP_SUMMARY"
          echo "scripts/update-memory.py exited non-zero — MEMORY.md was NOT updated." >> "$GITHUB_STEP_SUMMARY"
          echo "Check the 'Run memory updater' step log above for the traceback." >> "$GITHUB_STEP_SUMMARY"

      - name: Commit and push if MEMORY.md changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add MEMORY.md
          if ! git diff --staged --quiet; then
            DATE=$(date -u +%Y-%m-%d)
            git commit -m "chore: daily memory sync ${DATE}"
            git push
            echo "MEMORY.md updated and pushed."
          else
            echo "No changes to MEMORY.md — nothing to commit."
          fi
